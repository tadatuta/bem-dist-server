block('form')(
    tag()('form'),
    attrs()(function() {
        var ctx = this.ctx;

        return this.extend(applyNext(), {
            method: ctx.method,
            action: ctx.action
        });
    })
);
